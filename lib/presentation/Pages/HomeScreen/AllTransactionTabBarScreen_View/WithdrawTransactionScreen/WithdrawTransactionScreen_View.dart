import 'package:flutter/material.dart';

class WithdrawTransactionScreenView extends StatelessWidget {
  const WithdrawTransactionScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
